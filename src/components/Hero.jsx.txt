import React from "react";
import { motion } from "framer-motion";

function Hero() {
  return (
    <section id="hero" className="hero-section">
      <motion.h1
        initial={{ opacity: 0, y: -80 }}
        animate={{ opacity: 1, y: 0 }}
        transition={{ duration: 1 }}
        className="fancy-title"
      >
        Kefi Studio
      </motion.h1>
      <motion.p
        initial={{ opacity: 0, y: 40 }}
        animate={{ opacity: 1, y: 0 }}
        transition={{ delay: 0.5, duration: 1 }}
      >
        Velas artesanales con esencia y fantasía. Descubre nuestras colecciones y personaliza tu experiencia.
      </motion.p>
      <a href="#products" className="main-btn">Ver productos</a>
    </section>
  );
}

export default Hero;
